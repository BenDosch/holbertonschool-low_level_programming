#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - gets lenght of string
 * @s: string passed to function
 * Return: value of len
 */

int _strlen(char *s)
{
	int i;
	int len;

	for (i = 0; *(s + i) != '\0' ; i++)
	{
	}
	len = i;
	return (len);
}
