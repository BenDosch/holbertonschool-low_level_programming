#include "holberton.h"
#include <stdio.h>

/**
 * main - whole program that prints the name of the program.
 * @argc: number of arguments passed to c
 * @argv: array of arguments passed to main
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
