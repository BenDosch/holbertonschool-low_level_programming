#include "holberton.h"

/**
* _isupper - checks if c is uppercase
* @c: intiger passed to function representing a character
* Return: 1 if upper, 0 otherwise
*/

int _isupper(int c)
{
	if ((c >= 'A') || (c <= 'Z'}}
	{
		return (1);
	}
	else
	{
		return (o);
	}
{
